import 'package:flutter/material.dart';

class  CallsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text('Camera is there ...!!! ',
      style:TextStyle(fontSize: 20.0,
      fontWeight: FontWeight.bold,
      ) ,
      ),
      
    );
  }
}